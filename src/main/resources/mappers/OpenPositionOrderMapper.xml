<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dodo.ai_trader.service.mapper.OpenPositionOrderMapper">
    <resultMap id="BaseResultMap" type="com.dodo.ai_trader.service.mapper.entity.OpenPositionOrderEntity">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="client_order_id" property="clientOrderId" jdbcType="VARCHAR"/>
        <result column="order_id" property="orderId" jdbcType="VARCHAR"/>
        <result column="user_id" property="userId" jdbcType="VARCHAR"/>
        <result column="exchange" property="exchange" jdbcType="VARCHAR"/>
        <result column="symbol" property="symbol" jdbcType="VARCHAR"/>
        <result column="side" property="side" jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
        <result column="time_in_force" property="timeInForce" jdbcType="VARCHAR"/>
        <result column="quantity" property="quantity" jdbcType="DECIMAL"/>
        <result column="entry_price" property="entryPrice" jdbcType="DECIMAL"/>
        <result column="stop_price" property="stopPrice" jdbcType="DECIMAL"/>
        <result column="profit_target" property="profitTarget" jdbcType="DECIMAL"/>
        <result column="order_time" property="orderTime" jdbcType="TIMESTAMP"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="version" property="version" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, create_time, update_time, client_order_id, order_id, user_id, exchange, symbol, side, type, time_in_force,
        quantity, entry_price, stop_price, profit_target, order_time, status, version
    </sql>

    <insert id="insert" parameterType="com.dodo.ai_trader.service.mapper.entity.OpenPositionOrderEntity">
        insert into open_position_order (create_time, update_time, client_order_id, order_id, user_id, exchange, symbol, side, type, time_in_force,
        quantity, entry_price, stop_price, profit_target, order_time, status, version)
        values (now(), now(), #{clientOrderId}, #{orderId}, #{userId}, #{exchange}, #{symbol}, #{side}, #{type}, #{timeInForce},
        #{quantity}, #{entryPrice}, #{stopPrice}, #{profitTarget}, #{orderTime}, #{status}, #{version})
    </insert>

    <select id="selectProcessOrder" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from open_position_order
        where status in ('PENDING', 'FILLED')
    </select>

    <select id="queryByClientOrderId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from open_position_order
        where client_order_id = #{clientOrderId}
        and exchange = #{exchange}
    </select>

    <update id="updateStatus">
        update open_position_order
        set status = #{status},
        update_time = now(),
        version = version + 1
        where client_order_id = #{clientOrderId}
        and exchange = #{exchange}
        and version = #{version}
    </update>
</mapper>